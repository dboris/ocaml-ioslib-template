(executable
 (name lib)
 (libraries threads)
 (flags
  (:standard
   ;-verbose
   -noautolink
   -cclib -lunix
   -cclib -lthreadsnat))
 (foreign_stubs (language c) (names libwrap))
 (modes (native object)))

(rule
 (deps lib.exe.o)
 (targets libcaml.a)
 (action (run ar cq %{targets} %{deps})))
